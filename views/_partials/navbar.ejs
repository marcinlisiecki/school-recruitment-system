<nav class="navbar bg-dark navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">
            Rekrutacja
        </a>
        <% if (user) { %>
            <div class="text-light">
                Siemka, <%= user.name %>!

                <form action="/auth/logout" method="post" class="inline-form d-inline-block" style="margin-left: 1rem;">
                    <button type="submit" class="btn btn-danger">Wyloguj</button>
                </form>
            </div>
        <% } else { %>
            <div>
                <a href="/auth/login" class="btn btn-outline-light">Zaloguj</a>
                <a href="/auth/register" class="btn btn-primary">Zarejestruj się</a>
            </div>
        <% } %>
    </div>
</nav>